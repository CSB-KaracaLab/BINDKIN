#!/bin/tcsh
# This script removes the intermediate files generated by the script named "get_elec", from the HADDOCK output folders.

foreach i(100*)
	cd $i/structures/it1/water/
	rm valELEC pdblist elec
	cd ../../../../
end
