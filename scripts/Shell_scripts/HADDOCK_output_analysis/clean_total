#!/bin/tcsh
# This script removes the intermediate files generated by the scripts named "get_total_01" and "get_total_02", from the HADDOCK output folders.

foreach i(100*)
	cd $i/structures/it1/water/
	rm alldata*
	cd ../../../../
end
