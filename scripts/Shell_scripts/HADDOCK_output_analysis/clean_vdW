#!/bin/tcsh
# This script removes the intermediate files generated by the script named "get_vdW", from the HADDOCK output folders.

foreach i(BINDKIN*)
	cd $i/structures/it1/water/
	rm valVDW pdblist vdw
	cd ../../../../
end
